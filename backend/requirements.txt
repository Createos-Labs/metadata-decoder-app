# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7

# Decoder engine dependencies (same as the original desktop tool)
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0
rapidfuzz>=3.0

# GCP (storage only — Firestore replaced by PostgREST)
google-cloud-storage>=2.16
google-auth>=2.30

# Firebase Admin (token verification)
firebase-admin>=6.5

# HTTP client for PostgREST calls
requests>=2.32
